let colors=['purple','grey','yellow'];

let random =['Marrakech',12,13.5,false];

console.log(random);

// array methods

let join = colors.join('-');
let indexOf = random.indexOf(false);
let concat = random.concat(colors);
let push = colors.push('red');
// remove last item
colors.pop();

console.log(colors);
